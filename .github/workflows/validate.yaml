name: Validate firmware

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: firmware-validation
  cancel-in-progress: true

jobs:
  esphome:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - run: esphome config dbk.yaml
      - run: esphome config factory.yaml
      - run: esphome compile factory.yaml
      - run: esphome compile sim/pomodoro.yaml
      - uses: actions/upload-artifact@v5
        with:
          name: dbk-firmware
          path: |
            .esphome/build/dbk/.pioenvs/dbk/firmware.factory.bin
            .esphome/build/dbk/.pioenvs/dbk/firmware.ota.bin
      - uses: actions/upload-artifact@v5
        with:
          name: dbk-pomodoro-firmware
          path: |
            sim/.esphome/build/dbk-pomodoro/.pioenvs/dbk-pomodoro/firmware.factory.bin
            sim/.esphome/build/dbk-pomodoro/.pioenvs/dbk-pomodoro/firmware.ota.bin
