# Generated by tools/canvas-firmware.py; edit the Canvas source, not this file.
# Canvas example: DBK Readings (readings)
substitutions:
  device_name: dbk-canvas
  friendly_name: DBK Canvas
  initial_screen: '0'
  selected_screen: '0'
packages:
  hardware_and_gallery: !include ../gallery.yaml
display:
- id: !extend oled
  update_interval: 1s
  lambda: |-
    it.print(64, 0, id(font_header), TextAlign::TOP_CENTER, "DBK AIR");
    it.horizontal_line(0, 12, 128);
    it.print(8, 15, id(font_label), "PM2.5");
    it.printf(54, 14, id(font_value), TextAlign::TOP_RIGHT, "%.0f", id(pms_pm25).state);
    it.print(59, 15, id(font_label), "ug/m3");
    int bar = std::min(50, (int) id(pms_pm25).state);
    it.rectangle(8, 25, 112, 6);
    it.filled_rectangle(10, 27, bar * 2, 2);
